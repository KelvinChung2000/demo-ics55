# Least cell area of the 46 ABC strategies, measured on this tile's own
# netlist in sweep/strategies_RegFile.tsv: 6235.9 um^2, 164.1 um^2 below DELAY 4's 6400.0.
[synthesis]
strategy = "AREA 6"

# This tile's frozen die. The width is its fabric column's, which every type in
# that column is built to, and the height its row's; both are repeated on the
# other types sharing them so `flow.plan._claim` checks the pair agrees.
# Bound by RegFile: at 95.8 x 84.0 um it fills 0.8511 of its core and
# 0.8977 of the area DreamPlace places into, and routes 340 -> 75 -> 57 -> 39 -> 18 -> 3 -> 0
# with 0 DRC. Round r1, 2026-09-21.
[die]
width_micron = 95.8
height_micron = 84.0   # rows 1 to 14, the height the 82.5 anchor scaled them to
